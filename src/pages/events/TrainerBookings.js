import React from "react";
import Book from "../../components/buttons/Book";
import { MainWindow } from "../../styled-components";

export const TrainerBookings = (props) => {
    
  return (
    <MainWindow>
      <Book />
    </MainWindow>
  );
};
