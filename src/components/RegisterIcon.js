import Register from "../assets/register-green.png";

export const RegisterIcon = () => {
  return (
    <img
      src={Register}
      alt="Register new account"
      style={{
        width: "26px",
        height: "36px",
        marginBottom: "8px",
        marginTop: "2px",
      }}
    />
  );
};
