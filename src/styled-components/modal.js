import { popupMiddle } from "./mixins";

export const modalStyling = {
  position: "absolute",
  transform: "translate(-50%, -50%)",
  top: "50%",
  left: "calc(50% + 115px)",
  width: 500,
  bgcolor: "background.paper",
  boxShadow: 24,
  p: 5,
  borderRadius: "10px",
  // popupMiddle
};
